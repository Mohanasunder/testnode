import { ApplicationSource } from "../restify/HttpServer";

/**
* include all required keys which needs from client configuration
*/
export interface WebConfig {
    // /**
    //  * If authorization is enabled, it needs security data to be passed
    //  */
    // security?: {
    //     /**
    //      * Mongo DB Url for the security module, where users collection is stored
    //      */
    //     securityDBURL: string
    //     /**
    //      * Collection name where the user data and theirs privileages are stored
    //      */
    //     securityDBCollection: string,
    // }

    /**
 * Specify application source, where our db will be communicated
 * if it is aplx, specify db_url
 */
    applicationSource: ApplicationSource
    /**
     * If it is application source is applexus source, DB Url is mandatory 
     */
    DB_URL?: string
    /**
     * Logger folder path
     */
    LOG_PATH?: string
}